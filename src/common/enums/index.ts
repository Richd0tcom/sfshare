export enum UserRole {
    Admin = 'admin',
    Manager = 'manager',
    Employee = 'employee',
    Guest = 'guest',
    SuperAdmin = 'superadmin',
}

export enum PermissionLevel {
    Private = 'private',
    Public = 'public',
}

